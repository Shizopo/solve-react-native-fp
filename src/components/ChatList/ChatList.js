// @flow

import React from "react";
import {
  StyleSheet,
  ScrollView,
  View,
  Text,
  TextInput,
  Button,
  FlatList,
  Modal,
  ActivityIndicator,
} from "react-native";
import { style } from "../../styles/chatListStyles";

type Props = {};
type State = {};

const ChatList = () => {
  return <></>;
};

export default ChatList;
